import { Container, Typography } from "@mui/material";

const NewsPage = () => {
  return (
    <Container>
      <Typography variant="h3">Latest News</Typography>
      <Typography>All the latest updates from the club.</Typography>
    </Container>
  );
};

export default NewsPage;
